import { TeamsListPipe } from './teams-list.pipe';

describe('TeamsListPipe', () => {
  it('create an instance', () => {
    const pipe = new TeamsListPipe();
    expect(pipe).toBeTruthy();
  });
});
